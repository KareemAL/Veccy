package at.fhhgb.mtd.gop.veccy.data;

import at.fhhgb.mtd.gop.veccy.shapes.Shape;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class DoubleLinkedListTest {
/*
    @Test
    void clear() {
        DoubleLinkedList list = new DoubleLinkedList();
        list.append(new Shape( 0,0));
        list.append(new Shape(0,0));
        list.clear();

        assertNull(list.getHead());
        assertNull(list.getTail());
    }

    @Test
    void prepend() {
        DoubleLinkedList list = new DoubleLinkedList();
        list.prepend(new Shape(0,0));
        list.prepend(new Shape(2,0));
        list.prepend(new Shape(4,0));

        assertEquals(4, list.get(0).getX());
        assertEquals(2, list.get(1).getX());
        assertEquals(0, list.get(2).getX());
        assertEquals(0, list.get(1).getY());
    }

    @Test
    void append() {
        DoubleLinkedList list = new DoubleLinkedList();
        list.append(new Shape(1,0));
        list.append(new Shape(2,5));
        list.append(new Shape(3,0));

        assertEquals(1, list.get(0).getX());
        assertEquals(2, list.get(1).getX());
        assertEquals(3, list.get(2).getX());
        assertEquals(5, list.get(1).getY());
    }

    @Test
    void get() { // Funktioniert, wenn prepend/append funktioniert
    }

    @Test
    void size() {
        DoubleLinkedList list = new DoubleLinkedList();
        list.append(new Shape(1,0));
        list.append(new Shape(2,5));
        list.append(new Shape(3,0));
        int x = list.size();

        assertEquals(3, x);
    }
*/      // Unit Tests, that dont work after making Shape and abstract Class
    @Test
    void removeFirst() {
    }

    @Test
    void peekFirst() {
    }

    @Test
    void removeLast() {
    }

    @Test
    void peekLast() {
    }

    @Test
    void reverse() {
    }

    @Test
    void getHead() {
    }

    @Test
    void getTail() {
    }
}